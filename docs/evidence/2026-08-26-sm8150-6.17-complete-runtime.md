# SM8150 6.17 Complete Electronic Runtime Gate

Date: 2026-08-26

## Candidate

The clean migration source remains the seventeen-commit tree at
`cbb657877fd2ef7ad7925f8857ee81964e897819`, tree
`da7868a395cf9df795287bc4ac41489dce811cba`. Revision r8 changes no source
patch: it corrects the package configuration by enabling
`CONFIG_SM_CAMCC_8150=m` and makes `camcc-sm8150.ko` a required artifact.

| Artifact | Size | SHA256 |
|---|---:|---|
| r8 APK | 23,013,453 | `f4894e2512a4b8469f00579c68582bc2f2f62ca04fac7c38003c84f3950d7629` |
| Image | 31,492,608 | `ad1bd2af47dc14f9bdecf282e9d8887fcb5605568d58f7b65c0d9df5bf6670ae` |
| Hotdog DTB | 164,102 | `9d31fa35ecd38dfd560209e6fb7d93f32dbc71eadac2b349ed594b07a32b3b12` |
| `camcc-sm8150.ko` | - | `1862d62bef4afe92defca7eba18c02481f3e483640dec47442ccc2d7a1ae90f4` |
| raw boot image | 40,878,080 | `b7e741d973a5ea47f947aff9fc4d04fa3da7df1d792cc302aea671363d2b5cbc` |
| AVB boot image | 100,663,296 | `de20bf94620195958c54d7fe790d58754aef8650e68f4703d1916b92947aef54` |

The package has 841 modules with unique vermagic
`6.17.0-sm8150-hotdog-clean SMP preempt mod_unload aarch64`.

## Runtime

The full module tree was hash-checked on the writable rootfs. The AVB image
was written only to `boot_b` and its complete 100,663,296-byte prefix was read
back and matched before reboot. The phone returned to SSH on the new boot in
21 seconds without a key sequence and without exposing Qualcomm 9008 or 900e.

The camera clock omission was resolved exactly as predicted:

- `ad00000.clock-controller` bound to `camcc-sm8150`;
- both CCI platform devices bound to `i2c-qcom-cci`;
- CAMSS bound and exposed one media device, eight video nodes and twenty
  subdevices;
- IMX586, IMX481, IMX471 and S5K3M5 loaded;
- both LC898217XC focus actuators reported ready;
- the popup power domain reported ready while leaving the motor off;
- no camera device remained deferred.

At the same time SLPI, modem and ADSP were all running, QRTR service 400 was
present, and the two Hall sensors plus `elliptic_proximity` remained exposed
through IIO. The preceding r7 boot had already held this remoteproc and sensor
state for 30 samples over 300 seconds with no SLPI crash after the FastRPC
sensors-PD heap fix.

The r8 boot then passed its own 30-sample, 300-second monitor. Every sample
retained the same boot identity, one media device, eight video nodes, twenty
subdevices, all three remote processors in `running` state and QRTR service
400 present.

## A/B hypothesis

A read-only `qbootctl` check reports slot B active, successful and bootable;
slot A is also successful and bootable. Repeated direct writes of `boot_b` did
not exhaust an A/B retry counter. The earlier r4 first-boot failure followed
by a successful reset of the same image is therefore not explained by an
unbootable slot marker.

## Scope

`PASS` for complete electronic kernel enumeration. This does not claim camera
capture quality, autofocus movement, popup movement, flash synchronization or
other physical behavior. Those interactions remain reserved for the final
physical parity pass after the complete migrated stack is stable.

## Full image gate

The migration device package is now r35 and selects the r8 kernel package. It
also pins the matching parent device revision from both kernel subpackages and
ships the missing `sshd -> sshd.pam` executable name expected by OpenRC. A
fresh pmbootstrap install therefore needs neither manual module injection nor
an output-only SSH mutation.

The final full image is 14,096,007,168 bytes with SHA256
`b0adf3af7f39d891105cea72f9535f065449457aaea43b7d2c4ed0077f3ffbff`.
Its 4096-byte-sector GPT primary and backup headers have valid independent
CRCs; the backup header is at the final sector. `e2fsck -fn` passes on both
partitions. The p1 boot image is byte-identical to the separately preserved
100,663,296-byte AVB image with SHA256
`282d0d8becdd88e5e70515d9649ba9811aa16a22f026026b1157f3f70e9b3222`.
Its cmdline UUIDs match the two filesystem superblocks.

The rootfs contains only module tree `6.17.0-sm8150-hotdog-clean` with 841
modules, exact packages `linux-oneplus-hotdog-mainline617-clean=6.17.0-r8`
and `device-oneplus-hotdog=3-r35`, the PAM SSH binary link, Plasma Mobile
Wayland session and the complete radio/sensor/network/display service set.
This full image is offline-qualified but has not yet replaced userdata on the
phone.

Private runtime logs are retained under
`logs/2026-08-26-sm8150-617-complete-r7/` and
`logs/2026-08-26-sm8150-617-complete-r8/` and are not publication artifacts.
