Prepared downstream pmaports 4.14 USB/watchdog screen-diagnostic candidate
==========================================================================

Date: 2026-07-10 04:54 CEST

Candidate:

  /home/srobin/dev/hotdog/images/pmos-experiments/2026-07-10-045402-lineage414-pmaports-kernel-ttyautocycle-usbcarrierdiagwatchdog/boot-noefi-pmosdtb-watchdog-300s.img

Hashes:

  boot image: a77ed60d92ed55632842aa5a2b0e8f09dfb04bcfcbf63427f3f1f5530ef629c2
  kernel:     c6411a83cc004d52209b39d9ac6fa552d93b5be719bbaa0536060c78e4d4266e
  dtb pack:   9ed26b5cc289633ae1b98ce3212a084d673779fb188307a442f4922588032040
  initramfs:  98c017fc497a6eaa1bbbda377e8e0d6d752600c550a5bf7e3d182efdeaefffa8

Purpose:

  045402 supersedes 044715 as the automatic next hardware test. It keeps the
  pmaports lineage414 kernel, fixed entry12 /chosen ranges; DTB pack,
  initramfs tty-kmsg/fbprep follower, DRM console userspace hook, visible tty
  auto-cycle pages, USB carrier/up watchdog success, and sysrq-first timeout
  reboot.

  The new behavior is visible diagnosis of the USB/watchdog decision. The
  rootfs visible tty pages now print:

    - carrier for usb*/rndis*/enx*/eth* interfaces
    - operstate for those interfaces
    - interface MAC/address
    - hotdog_rescue_watchdog markers if visible in rootfs /tmp
    - hotdog_rescue_watchdog_sec from /proc/cmdline

Why:

  If the phone reaches a screen-visible rootfs but the host still sees no USB
  device, the next question is whether Linux thinks the USB link is up or whether
  only the gadget was configured. This candidate should show that directly on
  screen while still rebooting after 300s if carrier/up state never appears.

Build command:

  ./scripts/build-watchdog-bootimg.sh \
    --source-boot /home/srobin/dev/hotdog/images/pmos-experiments/2026-07-09-215005-lineage414-drmconsole-initramfs-rootwatchdog-v2/boot-noefi-pmosdtb-watchdog-300s.img \
    --watchdog-sec 300 \
    --watchdog-success usb \
    --kernel /home/srobin/dev/hotdog/build/apk-extract/linux-oneplus-hotdog-lineage414-r2/boot/vmlinuz \
    --dtb /home/srobin/dev/hotdog/build/apk-extract/linux-oneplus-hotdog-lineage414-r2/boot/dtbs/qcom/sm8150-oneplus-hotdog.dtb \
    --with-ramoops-cmdline \
    --fb-test \
    --drm-console \
    --drm-console-helper /home/srobin/dev/hotdog/build/hotdog-drm-console-aarch64 \
    --drm-console-userspace \
    --tty-kmsg-console \
    --visible-tty-shell \
    --visible-tty-autocycle \
    --extra-cmdline 'loglevel=8 fbcon=map:0 fbcon=font:VGA8x16 fbcon=vc:1-1' \
    --base 0x00000000 \
    --kernel-offset 0x00008000 \
    --ramdisk-offset 0x01000000 \
    --second-offset 0x00000000 \
    --tags-offset 0x00000100 \
    --dtb-offset 0x01f00000 \
    --outdir /home/srobin/dev/hotdog/images/pmos-experiments/2026-07-10-045402-lineage414-pmaports-kernel-ttyautocycle-usbcarrierdiagwatchdog

Offline checks:

  - Embedded watchdog uses /sys/class/net/*/carrier and operstate for USB
    success.
  - Embedded watchdog no longer references /sys/kernel/config/usb_gadget/g1/UDC
    or /sys/class/udc/* for success.
  - Embedded rootfs visible tty shell contains autocycle="1", "auto-cycle every
    12s", and the "usb/watchdog" status block.
  - Extracted hotdog-visible-tty-shell passes /bin/sh -n.
  - scripts/test-next-lineage414-simplefb-shell.sh now points to 045402.
  - scripts/validate-current-candidates.sh passes for 045402, 034200, and
    025400.
  - find scripts -type f -name '*.sh' -print0 | xargs -0 -n1 bash -n passes.
  - git diff --check passes.

Next hardware action:

  Let the existing wait-pmos-then-test watcher launch the wrapper if SSH returns,
  or run scripts/test-next-lineage414-simplefb-shell.sh once the phone becomes
  commandable. The current screen-text boot was started before this candidate
  existed, so it may still require manual recovery if its old watchdog already
  considered USB success.
