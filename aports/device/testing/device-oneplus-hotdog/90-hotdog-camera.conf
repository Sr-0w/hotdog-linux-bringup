# The SM8150 CAMSS graph cannot currently be shared with PipeWire's passive
# libcamera monitor. Camera applications use libcamera directly.
wireplumber.profiles = {
  main = {
    monitor.libcamera = disabled
  }
}
